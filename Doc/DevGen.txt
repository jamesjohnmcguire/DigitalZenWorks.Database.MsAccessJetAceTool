Overview
status
Today
Pending
Todo
Finished
Process
	¡ Problem definition
	¡ Requirements development
		* Definitions
		* Questions / Issues for requirements
		* specification
			Versions
	¡ Alternative Solutions
	¡ Construction planning
	¡ Software architecture, or high-level design
		*. use cases
		*. Versions
			Components
	¡ Detailed design
		*. Directory Structure
	¡ Coding and debugging
	¡ Unit testing
	¡ Integration testing
	¡ Integration
	¡ System testing
	¡ Corrective maintenance
Resources
Issues
Notes
Tasks

Overview
	Making easy, basic fixes to get to 'contacts'
	afterwards, work on restructure of DevGen
	Process
		In Iterations
			Determine milestone
			Put in known systematic or low-level changes
			system freeze
			Work upwards, when in high-level, either feature implention or bug-fixing
			no more systematic changes
			but note for next iternation
status
Today
Pending
Todo
Tasks
		remove language setting before add_update if not needed (maybe always)
		need to deal with add_update, language name fields as extended insert (see property_sub_types)
		set twice $this->template->set('languages', $languages);
		When needing complete language info, such as editing, add the TRUE	$social_restriction = $this->social_restrictions->get($id, TRUE);
		In field setting, may need to change: 	$fields[$index] = $social_restriction->$index;
<?php
if (!empty($id))
{
	?>
	<br />
	<a href="/admin/delete_certificate/<?php echo $id; ?>">
		<?php echo $this->lang->line('delete_certificate'); ?>
	</a>
	<br />
	<?php
}
?>
<br />

		checkbox value as 1
		if ($columns['preferredContactId'] == 0)
		{
			unset($columns['preferredContactId']);
		}
		password type for password
		<option value="0">------</option>
		remove enctype="multipart/form-data" from non-upload forms
		radio type needs label after not before
		radio type needs checked
		class accounts extends CI_Model
		if (empty($account))
		{
			if ($method == 'edit_account')
			{
				ShowMessage($CI->lang->line('invalid_user_id'),
					"$controller");
			}
		}
		improve file naming
		add header and constructor for controller
		work out HasTypes and source issue, that to use source, need HasTypes, but sometimes don't want HasTypes
	CI	fix missing section segment
	CI	When type is of checkbox or radio and there is a source, enumerate all source rows as checkboxesor radios
		add placeholder
		remove <option value="0">------</option>
		for delete controller, move sets within validation false
		for delete controller, the following is missing a section segment 						
			ShowMessage($this->lang->line('invalid_account_id'),
					'//list_accounts/');

	CI	too many $this->template->set('languages', $languages);
	CI	use knr_case template->set
	CI	fix select`a	test
	CI	$this->form_validation->set_rules(
	CI	$this->template->set('admin', TRUE);
	CI	$directory = $_SERVER['DOCUMENT_ROOT'] . '/PropertyImages/';
	CI	if ((empty($id)) || ($id == 0))
	CI	after generate fix empties
		add get function
		model names Name case for 'point_packages'
		complete model changes
	CI	why is section not set?
	CI	add ajax
		change list format
		add point package
		merge columns
	?	copyright
	C#	Add conditionals for UPDATE code
	C#	Fix CreateSln to only include relative paths
	C#	Add /// comments to table templates
	C#	fix formatting of table templates
	C#	apply php parsing fixes to cs
	document generic practice of including common/external projects
	implement generic practice of including common/external projects
	Change inclusion of externals projects to 'svn:externals'
	real main program file
	clean up and out prototypes
	update coding style
	SQL File parsing	comments before each table, how to handle
	Generize SQL table generation
		create a generic but diverse table with many types and options
		import it into the following
			access
			sql server
			my sql
			postgres
			sql lite
		dump it out of each
			compare and contrast
	C#	Finish implemenation hhiberate support
	PHP	DbLib Error in constructor
	PHP	check GetRow functionality
	PHP - test on unix system
	Update PHP code from fixes in other projects
	Test and Fix PhpBasic Login and Registration functionality
		Client: Me
		Initial estimate:	1 hr
		actual time: 26 Minutes (for testing)
	fix missing projects in output solution
	fix long lines in insert / update parameters
Finished
	CI	when field has source, add something like: $types = $this->Prefectures->get_types();
	CI	move inside validation $this->template->set('languages', $languages);
	CI	change add_update to use $fields
	'string' does not contain a definition for and no extension method 'ToProperCase' accepting a first argument of type 'string' could be found 
	Clean up notes file
	fix svn
		review notes from other projects on updating /creating svn repos
		backup
		devclean
		svnrm
		remove others I don't want in there
		update create script
		go
		DevTools	Codeigniter	fix add_update call
		DevTools	Codeigniter	add model
		DevTools	Codeigniter	ShowMessage($message
		DevTools	Ex = {"Could not load file or assembly 'Zenware.Common.Utils, Version=1.0.0.0, Culture=neutral, PublicKeyToken=4d499e85b92e4818' or one of its dependencies. An attempt was made to load a program with an incorrect format."}
		DevTools	Warning	2	There was a mismatch between the processor architecture of the project being built "MSIL" and the processor architecture of the reference "C:\Users\JamesMc\Data\Projects\Zenware\Dev\DevTools\Src\bin\Debug\Zenware.Common.DatabaseLibrary.dll", "x86". This mismatch may cause runtime failures. Please consider changing the targeted processor architecture of your project through the Configuration Manager so as to align the processor architectures between your project and references, or take a dependency on references with a processor architecture that matches the targeted processor architecture of your project.	c:\Windows\Microsoft.NET\Framework\v4.0.30319\Microsoft.Common.targets	1605	DatabaseUtilitiesNet
		DevTools	Warning	4	Found conflicts between different versions of the same dependent assembly.	c:\Windows\Microsoft.NET\Framework\v4.0.30319\Microsoft.Common.targets	1605	DevGen
Process
	¡ Problem definition
		In developing web sites, a lot of time is spent working on menial tasks.  These tasks take little thought and are often quite routine tasks, however they take a long time.  Furthormore, it is quite easy in doing these tasks not to adhere to the 'rules' of the project, thus causing inconsistencies, bugs and additional time fixing them.
	¡ Requirements development
		* Definitions
		* Questions / Issues for requirements
		* specification
			Automates a lot of these little processes.

			Versions
				Ver1
					Can be run from the command line

					Input
						For Web
							XML file that contains the following:
								* Project Name
								one or more of:
									* a table 'definition'
										* table name
										* columns
											* name
											* type
											* length
											* primary key
											* reference key
							NHibernate xml

							Html List Template file - contains information on displaying lists
							Html Item Template file - contains information on displaying items

						For App

					Output
						Standard Create SQL schema file
							drop and create DB
							for each table
								delete and create table

						For Web
							for each table
								list
									.aspx file
									.cs file
								view
									.aspx file
									.cs file
								insert
									.aspx file
									.cs file
								update
									.aspx file
									.cs file
								delete
									.aspx file
									.cs file
				Ver 2
					Also GUI interface

					Input
						SQL definition file
					Output
						SQL schema file
							stored procedures

						Additional SQL file for each table
							includes sample
								select
								insert
								update
								delete
							queries

						Older Visual Studio .net 2005/3 project files
	¡ Alternative Solutions
	¡ Construction planning
		* Review batch files
		Create prototype templates
		Create Project
		Develop Shell
		Integrate pieces
	¡ Software architecture, or high-level design
		*. Overview - description of model
			Basically, a classic MVC pattern.  However, what always confused me and made it more confusing, was it's implementation in a web context.  But by intepreting the browser as the 'controller', everything else becomes much simpler.  Like Thl, each view has it's html and absolute minimal code to support that view and then bulk of the code is in the model, which often will have typical classes
			so:
				<Page1>.aspx
					<Page1>.aspx.cs
				<Page2>.aspx
					<Page2>.aspx.cs
					.
					.
					.
				<PageN>.aspx
					<PageN>.aspx.cs
				media
					images
				include
					scripts
					styles
				Locality
				Common
					BasePage.cs
					DataBaseCore.cs
					Locality.cs
					UnitTest.cs

					<Table1>.cs
					<Table2>.cs
					.
					.
					.
					<Table1>.cs
				DB
					<Project>.sql
		*. use cases
			Initially, just 1:
				From the command line, I either fill in an xml file, or from a GUI, or with command line arguments with the project details, such as name and tables.
			Later on,
				Perhaps, customizing more to fit different 'genres' of websites.
		*. Versions
			Components
				GUI	App
					Describe Tables and / or pages
				Generater App
					The main thing
				Xml file input
					contains the project, table and page definition
				Output
					Full web project

				GUI	App
					version 2
				Generater App
					Main function
						ValidateArguments
						LogEvent
						ShowStatus

						new Project object
						Project.LoadConfig
						Project.Create;

					BaseProjectClass
						LoadConfig
						?SetTemplate
						ReadDefinition
						ReadTemplate
						?CreateFiles

					ProjectClass
						Create
							CreateProjectFiles
							for each Table
								new Table object
								Table.SetConfig
								if no sql input
									Table.AppendSqlFile
								Table.CreateFiles
							if Web
								CreateIndexFile
						CreateProjectFiles
							CreateSlnFile
							CreateCprojFile Dll
							CreateCprojFile App / Web
						CreateSqlFile
						LoadConfig
							Base.LoadConfig

					WorkSpace
						PhpProject
						DotNetProject
							DotNetApp
							DotNetWeb

					Table Class
						AppendSqlFile
						CreateFiles
							CreateObjectFiles
							if Web
								CreateWebFiles
							else
								CreateAppFiles

						CreateAppFiles
							ReadAppTemplates
							CreateListCsFile
							CreateListXamlFile
							CreateViewCsFile
							CreateViewXamlFile
							CreateInsertCsFile
							CreateInsertXamlFile
							CreateUpdateCsFile
							CreateUpdateXamlFile
							CreateDeleteCsFile
							CreateDeleteXamlFile
						CreateDataObjectFile
						CreateFactoryObjectFile
						CreateNHibernateFile
						CreateObjectFiles
							if no nhibernate
								CreateNHibernateFile
							CreateDataObjectFile
							CreateFactoryObjectFile
							CreateUnitTestFile
							CreateSqlMainFile
						CreateUnitTestFile
						CreateWebFiles
							ReadWebTemplates
							CreateListCsFile
							CreateListAspxFile
							CreateViewCsFile
							CreateViewAspxFile
							CreateInsertCsFile
							CreateInsertAspxFile
							CreateUpdateCsFile
							CreateUpdateAspxFile
							CreateDeleteCsFile
							CreateDeleteAspxFile
						ReadAppTemplates
						ReadWebTemplates
						SetConfig

	¡ Detailed design
		*. Directory Structure
			„¥„ŸGenWeb
			„   „¥„Ÿadmin
			„   „¥„ŸDoc
			„   „   „¤„ŸExternal
			„   „   „   „¤„ŸImplementing Front Controller in ASP_NET Using HTTPHandler_files
			„   „   „       „¥„Ÿleftframe_files
			„   „   „       „   „¥„Ÿdeeptree_files
			„   „   „       „   „¥„Ÿdtbar_files
			„   „   „       „   „¤„Ÿsearch_files
			„   „   „       „¥„Ÿrightframe_files
			„   „   „       „   „¥„Ÿcontentbar_files
			„   „   „       „   „¤„ŸImpFrontControllerInASP_files
			„   „   „       „       „¥„Ÿratings(1)_files
			„   „   „       „       „¤„Ÿratings_files
			„   „   „       „¤„Ÿtoolbar_files
			„   „¥„Ÿmisc
			„   „¥„ŸPrototypes
			„   „   „¥„ŸPageCtrl
			„   „   „¥„ŸProtoAspMvcCorp1
			„   „   „¥„ŸWebApp1
			„   „   „   „¥„Ÿimages
			„   „   „   „¤„Ÿ_private
			„   „   „¤„ŸWebApplication1
			„   „¥„Ÿsrc
			„   „   „¤„ŸGenWeb
			„   „¥„ŸSrcArchive
			„   „   „¥„ŸBatchFiles
			„   „   „¥„ŸDevGen
			„   „   „¤„ŸStripStrings
			„   „       „¤„ŸIMS HEALTH - Careers_files
			„   „¥„ŸTemplates
			„   „¤„Ÿtests
			„       „¤„Ÿaspmvc
		Main function
				Show start
				Validate arguements
				Show Message
				Create Project class
				ReadDefinition
				ReadTemplate
				Show Message
				CreateFiles
				Show Message

			ValidateArguments
				confirm existence of xml file
				confirm existence of html file
			ShowUsage
			ProcessArguments

			LogEvent
				<use as before>
			ShowStatus
				console.write
				LogEvent

		PrjBaseClass
			setDefinition
			setTemplate
			ReadDefinition
			ReadTemplate
			CreateFiles

		ProjectClass
			members
				sProjectName
				dsProject

			ReadDefinition
				read definition into DS
				for each table
					Create Table class
					Table.CreateFiles
					Table.ReadSQLDefinitionStream
			ReadTemplate
				ReadFile
				save
			CreateFiles
				CreateCprojFile
					gen guid for project
				CreateSlnFile
				CreateIndexFile
			CreateSqlFile
				create global info
				for each table
					Write info

		Table Class
			members
				sProjectName
				dtTable
				sTemplate

			setDefinition
			setTemplate
			CreateFiles
				CreateListCsFile
				CreateListAspxFile
				CreateViewCsFile
				CreateViewAspxFile
				CreateInsertCsFile
				CreateInsertAspxFile
				CreateUpdateCsFile
						copy template
						add sub header
						for each table element
							GenerateHtml( drElement )
					CreateInputStep
					CreateConfirmStep
					CreateSubmitStep
				CreateUpdateAspxFile
				CreateDeleteCsFile
				CreateDeleteAspxFile

				CreateSqlMainFile
				CreateSqlSelectSample
				CreateSqlSelectStoredProcedure
				CreateSqlInsertSample
				CreateSqlInsertStoredProcedure
				CreateSqlUpdateSample
				CreateSqlUpdateStoredProcedure
				CreateSqlDeleteSample
				CreateSqlDeleteStoredProcedure
	¡ Coding and debugging
	¡ Unit testing
	¡ Integration testing
	¡ Integration
	¡ System testing
	¡ Corrective maintenance
Resources
	How to use svn externals
		http://robm.me.uk/2006/08/13/managing-plugins-with-svn-externals
		http://wordaligned.org/articles/internal-subversion-externals
		svn propset svn:externals -F Externals.txt .
	TreeSurgeonUpdate.cmd
		copy /vush ..techs\win\CodeGen\TreeSurgeon\Core
		copy /vush ..techs\win\CodeGen\TreeSurgeon\TreeSurgeonConsole\Resources\Tools
	Pattern implementation
		After spending the last six months or so developing the prototype of a web application, I have moved onto refactoring and thus have begun looking at different patterns. The simplicity of the Page controller is obviously appealing, no brains - no headaches, but its usefulness is limited due to the event sequence. The event sequence is derived page - Page Load, Base Page - PageLoad, Derived page - overloaded method invocation via the callback. More useful is disabling the Page Load of the derived page giving the sequence Base Page - PageLoad and then the override callback. Using this sequence I can do high level security checks in the base page and then be assured that they have passed before the derived page is ever called, leaving only page specific security to deal with. This removes alot of repeated code in all pages deriving from the base, which is wanted. Another mod is the addition of a second callback, PostBack in addition to the PageLoad, thus giving a cleaner, more obvious sequence of events. Check the IsPostBack in the Base Page and then invoke the proper callback.
Issues
	To schema validate or not?
		pros
			Can use DataSets
			Gives added insurance to data file correctness
		cons
			hassle
			maybe overkill
Notes
	Debug Parameters
		..\..\config\Contacts.xml ..\..\config\ContactsX.sql
		..\..\config\HousingJapan.xml ..\..\config\HousingJapan.sql
data comes in from
	xml file
		types
	sql file
		types

output to
	php	types don't matter
	c#
		string
		int
		float
		bool
		object

Solution
	<Name>Shell	Exe
	<Name>UserInterface	Dll
	<Name>Bll
	<Name>Bll<Component>
	<Name>Dal
	<Name>Dal<Component>


	<Name>BusinessLogicLayer
	<Name>BllManager
	<Name>BusinessLogicLayerManager


			Contact.AdviceLevel	= "A";
			Contact.FriendLevel	= "B";

The tag does not exist in XML namespace 'local' The type local: was not found. Verify that you are not missing an assembly reference and that all referenced assemblies have been built.

http://sachabarber.net/?p=368
	Config
		..\..\config\Contacts.xml ..\..\config\ContactsX.sql
		..\..\config\OfJapan.xml
	Design
		Run
			Create
				CreateProjectFiles
					CreatePhpFiles
				for each table
					table.CreateFiles
						base.CreateFiles
	language
		CSharp
		PHP
	type
		Application	CSharp
		CodeIgniter	PHP
		Library	either
		Web	either
	Config
		..\..\config\Contacts.xml ..\..\config\ContactsX.sql
		..\..\config\OfJapan.xml
	Design
		Run
			Create
				CreateProjectFiles
					CreatePhpFiles
				for each table
					table.CreateFiles
						base.CreateFiles
	language
		CSharp
		PHP
	type
		Application	CSharp
		CodeIgniter	PHP
		Library	either
		Web	either
	Tasks
		Finished
			DevTools	Codeigniter	fix add_update call
			DevTools	Codeigniter	add model
			DevTools	Codeigniter	ShowMessage($message
			DevTools	Ex = {"Could not load file or assembly 'Zenware.Common.Utils, Version=1.0.0.0, Culture=neutral, PublicKeyToken=4d499e85b92e4818' or one of its dependencies. An attempt was made to load a program with an incorrect format."}
			DevTools	Warning	2	There was a mismatch between the processor architecture of the project being built "MSIL" and the processor architecture of the reference "C:\Users\JamesMc\Data\Projects\Zenware\Dev\DevTools\Src\bin\Debug\Zenware.Common.DatabaseLibrary.dll", "x86". This mismatch may cause runtime failures. Please consider changing the targeted processor architecture of your project through the Configuration Manager so as to align the processor architectures between your project and references, or take a dependency on references with a processor architecture that matches the targeted processor architecture of your project.	c:\Windows\Microsoft.NET\Framework\v4.0.30319\Microsoft.Common.targets	1605	DatabaseUtilitiesNet
			DevTools	Warning	4	Found conflicts between different versions of the same dependent assembly.	c:\Windows\Microsoft.NET\Framework\v4.0.30319\Microsoft.Common.targets	1605	DevGen
			add ajax
			add ajax method
			add point package
			why is section not set?
			complete model changes
			fix select`a	test
			$this->form_validation->set_rules(
			$this->template->set('admin', TRUE);
			$directory = $_SERVER['DOCUMENT_ROOT'] . '/PropertyImages/';
						if ((empty($id)) || ($id == 0))
			add get function
			remove random 't' in <option> and select

		'string' does not contain a definition for and no extension method 'ToProperCase' accepting a first argument of type 'string' could be found 
		model names Name case for 'point_packages'
		break up and _ names (ex: ManagingSystems to managing_systems)
		Move language elements from template into code
		update add_update from certificates
		check DOCUMENT_ROOT
		<img height="20px" width="20px" src="/images/Certificates/<?php echo $certificate->picture; ?>">
		handle picture edit value
		private $table_name	= 'managing_systems';
		indents on if ($this->form_validation->run() == FALSE)
		if no file upload
		generate lang prep statements

		after generate fix empties
		change list format
		merge columns
		IsAdmin?
		$types = $this->property_sub_types->get_all();
		value="<?php echo $name[$language->id]; ?>" />
		add_update language names
		fix select foreach types
		-- joins
 
how much more do i improve DevGen until going to Contacts?
	Fix some basic low-lying fruit, then move on


Change everything over to NHibernate
	Need to make sample
	Need to incorporate samples into DevGen
finish automatic generation of classes based on tables
finish TimeTracker

Template - Dll - It's essentially handles the 'model' of MVC
	DataLogicLayer
		FactoryObjects
	BusinessLogicLayer
		Class/TableObjects

DevGen
	for each table
		generate hibernate xml file
		generate data object class
		generate factory class
options
	enumerate all of them
	add transform to knr in php
	use camelCase everywhere
	use kn everywhere
	
						if (firstColumn == false)
						{
							postDeclarations += ",\r\n";
						}
						postDeclarations += "\t\t\t\t'" + Name +
								"' => $this->input->post('" + Name +
								"', TRUE)";
						}
						if (!FormType.ToLower().Equals("hidden"))

Add updateResources to build
Rename refs to Zenware
change database constructor
Error loading directive.properties! Something is very wrong if these properties aren't being located. Either your Velocity distribution is incomplete or your Velocity jar file is corrupted!
Cannot get NVelocity Runtime default properties!
Could not load file or assembly 'log4net, Version=1.2.0.33710, Culture=neutral,PublicKeyToken=null' or one of its dependencies. The located assembly's manifest definition does not match the assembly reference. (Exception from HRESULT: 0x80131040)
Change back assuming primary key is 'id'
Before processing sql contents, regex all multiple whitespaces into 1 whitespace
Finished
	find out why sln file isn't being saved	ex.Message = "Could not find a part of the path 'c:\\Users\\JamesMc\\AppData\\Local\\temp\\Contacts\\Contacts\\Src\\Contacts.sln'."
Notes
	C:\Users\JamesMc\Data\Projects\Zenware\Dev\DevTools\Src\External\TreeSurgeon\Core\Resources\Templates\2008
	C:\Users\JamesMc\Data\Projects\Zenware\Dev\DevTools\Src\External\TreeSurgeon\lib
Run
	[DevGenAppliction]ValidateArguments
	[DevGenAppliction]ConfigFile config = Project.LoadConfig(confileFilePath);
	[DevGenAppliction]ProjectLanguages language = config.GetProjectLanguageType();
	[DevGenAppliction]ProjectTypes type = config.GetProjectType();
	[DevGenAppliction]project = new ProjectCSharpLibrary(config);
	[DevGenAppliction]project.LoadSqlFile(confileFilePath);
	project.Create();
		[ProjectCSharp]
		tableNames = base.Create();
			[Project]
			CreateProjectFiles
				[ProjectCSharp]
				base.CreateProjectFiles();
					[Project]
					new TreeSurgeonPlus
		treeSurgeon.GeneratePlus

	string sql = "INSERT INTO ConflictGroups " +
		"(`id`) VALUES " +
		"('" + conflictGroup.Id + "')";
/// Represents a Countrie object.

config with validation should only be called once
why does type come to be web?
Config\Contacts.xml Config\ContactsX.sql
private int CommandCreateUpdateStatements(
	string[] args)
{
	int ReturnCode = -1;

	if (3 < args.Length)
	{
		string SqlFile = args[1];
		string UpdateCsFile = args[2];
		string TestCsFile = args[3];

		if ((File.Exists(SqlFile)) && (File.Exists(UpdateCsFile)) && (File.Exists(TestCsFile)))
		{
			StorageContainers DatabaseHelper = new StorageContainers();

			//DatabaseHelper.CreateUpdateStatments(@"c:\data\admin\contacts\ContactsX.sql", @"c:\out.cs", @"c:\OutTest.cs");
			DatabaseHelper.CreateUpdateStatments(SqlFile, UpdateCsFile, TestCsFile);

			ReturnCode = 0;
		}
	}

	return ReturnCode;
}

Config\Contacts.xml Config\Contacts.sql